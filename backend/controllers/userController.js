/**
 * Legacy user controller placeholder.
 *
 * The previous implementation depended on role and department metadata, which
 * has now been fully removed in favor of screen-based access control.
 * If user management endpoints are needed inside this module in the future,
 * they should be rebuilt from scratch using the new screen-rights model.
 */

